export default ImageResizer;
declare class ImageResizer {
    static resize(url: any, options?: {
        maxWidth: number;
        maxHeight: number;
        contentType: string;
        quality: number;
    }): Promise<any>;
    static calculateSize(img: any, maxWidth: any, maxHeight: any): any[];
}
